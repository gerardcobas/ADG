<div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title">
          	{% if tam is sameas(2) %} Afegir nou fons
          	{% elseif tam is sameas(3) %} Afegir nou fubfons
          	{% elseif tam is sameas(4) %} Afegir nou grup de series
          	{% elseif tam is sameas(5) %} Afegir nova serie
          	{% elseif tam is sameas(6) %} Afegir nova unitat documental composta
          	{% elseif tam is sameas(7) %} Afegir nova unitat documental simple
          	{% elseif tam is sameas(8) %} Afegir nova unitat d'instalació
          	{% endif %}
          </h4>
        </div><!-- /.header -->
        
		<form action="{{ path("guia_nou_confirmar") }}" method="post">
			<div class="modal-body">
		      	{% set len =  info.nivell | length%}
		      	Inici:{{ info.nivell | slice(0, len-2) }}
		      	Canviable:{{ info.nivell | slice(len-2, len) }}
		      	
		    	<p>Informació actual del nivell {{ info.nivell }}, modificar per afegir un de nou.</p>
		      	
		      	<div class="input-group">
					<span class="input-group-addon">Nivell: <strong>{{ info.nivell | slice(0, len-2) }}</strong></span>
					<input type="text" class="form-control" pattern="^([a-zA-Z0-9]*\.)$" 
						title="Només pot tenir números i/o lletres i finalitzar amb un punt." 
						value="{{ info.nivell | slice(len-2, len) }}"
					>
				</div>
				
		        <label>{{ info.titol }}</label>
				<div class="form-group">
				  <input type="text" class="form-control" name="titol" required="required">
				</div>
		        <div class="space10"></div>
		        <label>Contingut</label>
		       	<div class="form-group">
				  <input type="text" class="form-control" rows="3" name="contingut" required="required" >
				</div>
				<input type="text" class="form-control" pattern="^([0-9]*\.)*$" title="Només pot tenir números, lletres i punts. Finalitzat amb un punt.">
				  
	        </div>
	        <div class="modal-footer">
	          <button class="btn btn-default" data-dismiss="modal">Tancar</button>
	          <button class="btn btn-success" type="submit">Guardar</button>
	        </div>
		</form><!-- /.form -->

      </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->