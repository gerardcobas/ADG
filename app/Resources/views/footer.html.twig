		    <div class="footer-wrapper">
		    	<footer>
					<h4>Arxiu diocesà de Girona</h4>
					<a class="foot" href="{{ path('contacte') }}">
						<i class="icon-envelope"></i>
						Contacte
					</a>
					
					<a class="foot" href="{{ path('normativa') }}">
						<i class="icon-file-text-alt"></i>
						Normativa
					</a>
					
					<a class="foot" href="{{ path('info_web') }}">
						<i class="icon-info-sign"></i>
						Web
					</a>
					
            		{% if is_granted("ROLE_ADMIN") %}
						<a class="foot" href=" {{ path('admin') }}">
							<i class="icon-user"></i>
							Administrar
						</a>
           			{% elseif is_granted("ROLE_USER") %}
					{% else %}
					    <a class="foot" href="{{ path('fos_user_security_login') }}">
							<i class="icon-signin"></i>
							Autentificació
						</a>
					{% endif %}
				</footer><!-- /.footer -->
			</div>
		  </div>
